# Cloudflare Pages configuration (reference).
# Project builds from GitHub main branch: `npm run build` → ./dist
name = "kl-annuaire"
pages_build_output_dir = "dist"
compatibility_date = "2026-01-01"
